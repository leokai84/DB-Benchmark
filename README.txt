Content:
1) it.db.benchmarks package with 4 classes:
	QueryManager.java
	Evaluation.java (contains main method)
	EvaluationInsert.java
	EvaluationSelect.java

2) pom.xml file containing dependency for postgresql
3) create-table.sql with postgresql code to create a simple table

To run project, you must configure some arguments as explained in the main method.
